Given a metric candidate, produce a business definition and grain.
Return JSON only:
{
  "metric_name": "string",
  "definition": "string",
  "grain": "string",
  "dimensions": ["string"]
}
Rules:
- Use only provided context.
- Do not change metric_name.
The runtime will append the metric candidate payload below under a `Metric candidate and columns` section.
