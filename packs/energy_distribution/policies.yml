industry: energy_distribution
version: 1

policies:
  - name: sbu_exclusion
    applies_to: all
    filter:
      column: sbu_name
      operator: not_in
      values: ["Common", "Mumbai Ref", "Renewable Energy", "Visakh Ref"]
